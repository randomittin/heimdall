{
  "version": "1.0.0",
  "gate_type_ranking": [
    "differential",
    "trace-diff",
    "verdict",
    "property",
    "example"
  ],
  "oracles": {
    "rr-multitenant-isolation": {
      "gate_type": "example",
      "description": "Multi-tenant cross-tenant-denial oracle. Grades a candidate isolation module (ES module exporting `default evaluate(attackId)`) against a FIXED all-DENY acceptance table of 25 enumerated cross-tenant attacks drawn from the RED-line table in docs/specs/2026-07-03-rr-isolation-invariants.md, and asserts EVERY attack is DENIED. Covers repo<->team authz (IDOR via repo slug, INV-11), server-derived team_id (INV-1: a wire team_id is never operative), per-team cred/queue/installation partition (INV-2/3, A5), the enqueue-only credential-free public surface (INV-6), the owner-only god aggregate (INV-GOD: /god/* 404s on the PUBLIC surface for every caller and every team secret; owner PKI + valid signature + IAM on the gated service; partitions enumerated server-side even for the owner; IAP assertions VERIFIED, never the edge trusted), dashboard login sessions (INV-LOGIN: server-derived teams, TTL enforced, mint signature verified, never owner), chat-handle binding (INV-CHAT: a bare chat_id is never an identity), and zero-touch auto-team join (INV-AUTOTEAM: collaborator check, gh identity re-derivation, public write threshold, initiate admin/push, HAID possession, repo->team convergence). gate_type is `example`, NOT `differential`: the truth source is a fixed input->expected-output table, not an independently-authored reference implementation recomputed over the same inputs. Gate strength comes instead from the falsifiability sweep — `bin/falsify` requires the golden candidate GREEN and all 23 single-gate-drop mutants RED (score 1.0), so no isolation gate in the model is decorative.",
      "domain_signals": [
        "rr-multitenant-isolation",
        "multi-tenant isolation",
        "cross-tenant",
        "tenant partition",
        "cross-partition read",
        "IDOR",
        "server-derived team_id",
        "team_covers_repo",
        "enqueue-only public surface",
        "INV-GOD",
        "god roster",
        "/god/",
        "INV-AUTOTEAM",
        "repo team auto-join"
      ],
      "assets": [
        {
          "name": "acceptance-oracle",
          "kind": "in-repo-dataset",
          "purpose": "The fixed {attackId -> \"DENY\"} truth table (fixtures/golden/acceptance.json, 25 cases, every expected DENY), transcribed from the spec's RED-line attack table rather than computed by any implementation. It is applied identically to the golden candidate and to every mutant, and it shares no code path with fixtures/model.mjs (the shared codebase golden and mutants are cut from). Weakening it — dropping an attack case — is exactly how test/heimdall-rr-isolation-oracle.test.sh proves the table is load-bearing: the mutant that only flips that case then SURVIVES and the falsify score drops below 1.0.",
          "dir": "evals/oracles/rr-multitenant-isolation/fixtures/golden"
        },
        {
          "name": "gate-dropped-mutant-corpus",
          "kind": "in-repo-dataset",
          "purpose": "23 committed mutants, each the shared isolation model with EXACTLY ONE isolation gate dropped, pinned via manifest.json to the single attack it flips DENY->ALLOW. The falsifiability sweep requires all 23 KILLED plus the golden GREEN, which is the meta-proof that each registered gate actually stops a real cross-tenant attack. Two acceptance cases (enqueue-cross-partition, G2-team-secret-god) have no dedicated mutant of their own — they exercise gates already pinned by an earlier case (serverDerivedTeam via A1-spoof-team-id, publicSurfaceRoute via G1-god-on-public), so they are graded on every run but are not the first_divergence for any mutant.",
          "dir": "evals/oracles/rr-multitenant-isolation/fixtures/mutants"
        }
      ],
      "gate_command": "bin/falsify rr-multitenant-isolation --assert-score 1.0",
      "reference": {
        "independent": true,
        "kind": "in-repo-dataset",
        "dir": "evals/oracles/rr-multitenant-isolation/fixtures/golden"
      }
    },
    "team-copilot": {
      "gate_type": "differential",
      "description": "Unified team co-piloting oracle (REDUM TEAM-LENS arm). Folds an identical seeded teammate work-state stream through the REAL redum team lens (bin/lib/redum.factor_for_task with team_lens=True, driven over a real on-disk claim+checkpoint ledger) and an independently-authored reference fold, normalizes both to a common comparable partition (the team-candidate roster [surface, haid, human, class, adoptable] \u2014 NOT the raw records), and asserts they are identical across all seeds. Catches whole-aggregate co-piloting bugs a per-record property check passes: a cross-team read (RT-ISO), a reaped-vs-in-flight misclassification (RT-CLASS, the code\u222awork bridge: don't-reinvent live work / don't-redo dropped work), a dropped teammate row (RT-DROP), or a teammate's goal prose leaking into a surfaced candidate (RT-LEAK). The team residual signal at the commit gate is WARN-only (never blocks); the local same-repo redum duplicate gate is unchanged (still hard-blocks).",
      "domain_signals": [
        "team-copilot",
        "redum team lens",
        "teammate-in-flight",
        "adoptable-dropped",
        "code union work bridge",
        "team_checkpoint_v1",
        "claimed_surfaces",
        "factor_for_task"
      ],
      "assets": [
        {
          "name": "reference-fold",
          "kind": "separate-agent",
          "purpose": "Independently authored, spec-only recomputation of the team-candidate roster over a seeded teammate work-state stream. Imports nothing from bin/lib/* (every rule reproduced from INVARIANTS.md), so it shares no code path with the implementation under test (bin/lib/redum.py team lens).",
          "dir": "evals/oracles/team-copilot"
        },
        {
          "name": "seeded-workstate-stream",
          "kind": "in-repo-dataset",
          "purpose": "Deterministic seeded teammate work-state stream generator (reference.generate_stream). Same seed => byte-identical stream; the distribution exercises all three classes (in-flight / adoptable-dropped / completed) plus a cross-team slice that must be excluded (RT-ISO).",
          "dir": "evals/oracles/team-copilot"
        }
      ],
      "gate_command": "evals/oracles/team-copilot/gate.sh --differential --seeds 200",
      "reference": {
        "independent": true,
        "kind": "separate-agent",
        "dir": "evals/oracles/team-copilot"
      }
    },
    "team-checkpoint": {
      "gate_type": "differential",
      "description": "Shared team-checkpoint roster oracle (TEAM MODE P4). Folds an identical seeded per-HAID checkpoint stream through the implementation (bin/lib/checkpoint_share.fold_stream) and an independently-authored reference fold, normalizes both to a common comparable partition (served roster set keyed by HAID + security-exclusion count \u2014 NOT the raw records), and asserts they are identical across all seeds. Catches whole-aggregate privacy/roster bugs a per-record property check passes: a security-lane leak (INV-C), a secret leak (INV-B), a consent-off leak (INV-D), an absolute-path leak (INV-A), or a dropped/duplicated/misattributed teammate (INV-G).",
      "domain_signals": [
        "team_checkpoint_v1",
        "shared checkpoint",
        "checkpoint roster",
        "team checkpoint",
        "resumable snapshot",
        "team-checkpoint"
      ],
      "assets": [
        {
          "name": "reference-fold",
          "kind": "separate-agent",
          "purpose": "Independently authored, spec-only recomputation of the checkpoint roster fold over a seeded synthetic checkpoint stream. Imports nothing from bin/lib/* (every constant and rule reproduced from INVARIANTS.md), so it shares no code path with the implementation under test.",
          "dir": "evals/oracles/team-checkpoint"
        },
        {
          "name": "seeded-checkpoint-stream",
          "kind": "in-repo-dataset",
          "purpose": "Deterministic seeded per-HAID checkpoint stream generator (reference.generate_stream). Same seed => byte-identical stream; the distribution is skewed so a slice is security-sensitive (INV-C exclusion) and a slice is consent-off (INV-D skip), the rest served.",
          "dir": "evals/oracles/team-checkpoint"
        }
      ],
      "gate_command": "evals/oracles/team-checkpoint/gate.sh --differential --seeds 200",
      "reference": {
        "independent": true,
        "kind": "separate-agent",
        "dir": "evals/oracles/team-checkpoint"
      }
    },
    "issue-collection": {
      "gate_type": "differential",
      "description": "Anonymized issue-collection k-anon aggregate oracle. Feeds an identical seeded issue_v1 stream through the implementation aggregate (bin/lib/cp_issue_aggregate.py) and an independently-authored reference aggregator, normalizes both to a common comparable partition (served bucket set + suppressed bucket set + security-exclusion count \u2014 NOT the raw envelopes, which differ by design), and asserts they are identical across all seeds. Catches whole-output k-anon/bucketing/security-exclusion bugs a per-bucket property check passes.",
      "domain_signals": [
        "issue_v1",
        "k-anonymity",
        "anonymized issue",
        "issue aggregate",
        "signature bucket",
        "issue-collection"
      ],
      "assets": [
        {
          "name": "reference-aggregator",
          "kind": "separate-agent",
          "purpose": "Independently authored, spec-only recomputation of the k-anon issue aggregate over a seeded synthetic issue stream. Imports nothing from bin/lib/* (every constant and computation reproduced from INVARIANTS.md), so it shares no code path with the implementation under test.",
          "dir": "evals/oracles/issue-collection/reference"
        },
        {
          "name": "seeded-issue-stream",
          "kind": "in-repo-dataset",
          "purpose": "Deterministic seeded issue_v1 stream generator (reference/aggregate_ref.generate_stream). Same seed => byte-identical stream driving the differential comparison; the distribution is skewed so some signature buckets clear the k>=10 distinct-team floor and some do not, and a slice is security-sensitive (exercising INV-F exclusion).",
          "dir": "evals/oracles/issue-collection/reference"
        }
      ],
      "gate_command": "evals/oracles/issue-collection/gate.sh --differential --seeds 200",
      "reference": {
        "independent": true,
        "kind": "separate-agent",
        "dir": "evals/oracles/issue-collection/reference"
      }
    },
    "emulator-gb": {
      "gate_type": "trace-diff",
      "description": "Game Boy (DMG / LR35902 CPU) emulator oracle. Verifies an implementation by diffing its per-instruction execution trace against a known-correct reference trace, then asserting the standard Blargg ROM serial-output pass markers.",
      "domain_signals": [
        "*.gb",
        "gameboy",
        "blargg",
        "opcode",
        "LR35902"
      ],
      "assets": [
        {
          "name": "blargg-cpu-instrs",
          "kind": "external-dataset",
          "purpose": "Blargg's cpu_instrs test ROM suite \u2014 the de-facto correctness battery for CPU opcode/flag behavior. Each sub-ROM prints PASS/FAIL over the serial link.",
          "url": "https://github.com/retrio/gb-test-roms/tree/master/cpu_instrs",
          "fetch": "git submodule or curl per-rom; do NOT copy ROM bytes into this repo"
        },
        {
          "name": "gameboy-doctor-truth-traces",
          "kind": "external-dataset",
          "purpose": "gameboy-doctor reference CPU state logs (one line per instruction: registers + PC + opcode bytes). The trace-diff compares the implementation's emitted log line-for-line against these.",
          "url": "https://github.com/robert/gameboy-doctor",
          "fetch": "clone reference repo; truth logs live under its truth/ tree, keyed by test rom number"
        }
      ],
      "gate_command": "evals/oracles/emulator-gb/gate.sh",
      "reference": {
        "independent": true,
        "kind": "external-dataset"
      }
    },
    "exchange-lob": {
      "gate_type": "differential",
      "description": "Limit-order-book matching-engine oracle. Replays seeded order flow through both the implementation and an independent naive O(n^2) reference matcher, then asserts the resulting fills, book state, and event streams are identical across all seeds.",
      "domain_signals": [
        "order book",
        "matching engine",
        "limit order",
        "price-time priority"
      ],
      "assets": [
        {
          "name": "reference-matcher",
          "kind": "separate-agent",
          "purpose": "Independently authored naive O(n^2) price-time-priority matching engine. Deliberately simple (correctness over speed) so it shares no code path with the optimized implementation under test.",
          "dir": "evals/oracles/exchange-lob/reference"
        },
        {
          "name": "seeded-lob-replay",
          "kind": "in-repo-dataset",
          "purpose": "Deterministic seeded order-flow generator + recorded replay fixtures. Each seed produces a reproducible stream of limit/market/cancel orders driving the differential comparison.",
          "dir": "evals/oracles/exchange-lob/replay"
        }
      ],
      "gate_command": "evals/oracles/exchange-lob/gate.sh --differential --seeds 200",
      "reference": {
        "independent": true,
        "kind": "separate-agent",
        "dir": "evals/oracles/exchange-lob/reference"
      }
    },
    "raytracer-calib": {
      "gate_type": "verdict",
      "calibration_only": true,
      "description": "Ray-tracer rendering oracle used only to calibrate the gate harness (not as a pass/fail gate for shipped work). Renders a fixed scene and issues a verdict by comparing output against a reference image via SSIM at a high similarity threshold.",
      "domain_signals": [
        "raytracer",
        "ray tracing",
        "SSIM",
        "render",
        "path tracing"
      ],
      "assets": [
        {
          "name": "reference-render",
          "kind": "in-repo-dataset",
          "purpose": "Golden reference render of the calibration scene at fixed resolution/seed. The verdict gate scores the implementation's render against this image with SSIM.",
          "dir": "evals/oracles/raytracer-calib/reference"
        }
      ],
      "gate_command": "evals/oracles/raytracer-calib/gate.sh --ssim-threshold 0.99",
      "reference": {
        "independent": true
      }
    },
    "symbol-reuse": {
      "gate_type": "differential",
      "description": "Cross-project symbol-reuse oracle (ponytail rung-2: 'already exists? reuse it'). Folds an identical seeded stream of symbol declarations through the implementation detector (bin/lib/redum.detect_symbol_reuse, via synthesized source) and an independently-authored reference fold, normalizes both to a common comparable partition (the blocked/advised/ok classification \u2014 NOT the raw findings), and asserts they are identical across all seeds. Catches whole-partition classification bugs a per-symbol property check passes: a missed exact function/type re-declaration (SR-C/SR-D), a false-flagged unique symbol (SR-H), an ignored opt-out marker (SR-B), or a cross-file duplicate missed by a file-local index (the project-wide guarantee).",
      "domain_signals": [
        "symbol_reuse_v1",
        "cross-project reuse",
        "symbol reuse",
        "duplicate symbol",
        "re-declared symbol",
        "canonical symbol",
        "symbol-reuse"
      ],
      "gate_command": "evals/oracles/symbol-reuse/gate.sh --differential --seeds 200",
      "reference": {
        "dir": "evals/oracles/symbol-reuse",
        "independent": true,
        "kind": "separate-agent"
      },
      "assets": [
        {
          "dir": "evals/oracles/symbol-reuse",
          "kind": "separate-agent",
          "name": "reference-fold",
          "purpose": "Independently authored, spec-only recomputation of the symbol-reuse classification over a seeded synthetic symbol-declaration stream. Imports nothing from bin/lib/* (every rule reproduced from INVARIANTS.md), so it shares no code path with the implementation under test."
        },
        {
          "dir": "evals/oracles/symbol-reuse",
          "kind": "in-repo-dataset",
          "name": "seeded-symbol-stream",
          "purpose": "Deterministic seeded symbol-declaration stream generator (reference.generate_stream). Same seed => byte-identical stream; the distribution cycles eight class templates so every seed exercises blocked (exact fn/type), advised (structural/same-name/const), ok (unique + opt-out), and dropped (ignored path)."
        }
      ]
    },
    "triage-coord": {
      "gate_type": "differential",
      "description": "Team issue-triage co-piloting oracle. Folds an identical seeded multi-HAID issue-pick stream (variable-latency interleaved) through the REAL triage claim path (bin/lib/issue_claim.simulate_claim_stream over a real on-disk HAID-shared claim ledger) and an independently-authored reference fold, normalizes both to the served-claim sequence, and asserts identical across all seeds. Catches whole-sequence triage bugs a per-record check passes: a double-pick race (two teammates claim one issue), a cross-team read, a stale claim not reaped. Routing is suggest-only; the claim gate is HAID-attributed so two teammates never triage/fix the same issue.",
      "domain_signals": [
        "triage-coord",
        "issue triage",
        "issue-claim",
        "double-pick",
        "suggest-only routing",
        "reap-adopt",
        "issue_claim",
        "issue_queue"
      ],
      "assets": [
        {
          "name": "reference-fold",
          "kind": "separate-agent",
          "purpose": "Independently authored spec-only recomputation of the served-claim sequence; imports nothing from bin/lib/*.",
          "dir": "evals/oracles/triage-coord"
        },
        {
          "name": "seeded-pick-stream",
          "kind": "in-repo-dataset",
          "purpose": "Deterministic seeded interleaved multi-HAID issue-pick stream; exercises double-pick, cross-team, stale-reap.",
          "dir": "evals/oracles/triage-coord"
        }
      ],
      "gate_command": "evals/oracles/triage-coord/gate.sh --differential --seeds 200",
      "reference": {
        "independent": true,
        "kind": "separate-agent",
        "dir": "evals/oracles/triage-coord"
      }
    }
  }
}