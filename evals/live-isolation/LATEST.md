# Live isolation verification receipt

Generated by `bin/heimdall-live-verify` — do not hand-edit. This file is a STABLE path,
overwritten on every run, so drift against the deployed control plane shows up as a plain
`git diff`. Every past receipt is in git history: `git log -p evals/live-isolation/LATEST.md`.

| field | value |
|---|---|
| verdict | **PASS** |
| run_utc | `2026-08-02T20:00:05Z` |
| git_sha | `663a8455cb10d12c22b65b922ec345f54db4eec0` |
| git_dirty | `false` |
| control_plane | `https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app` |
| site | `https://runheimdall.dev` |
| project | `github.com/randomittin/heimdall` |
| routes_registered | `35` |
| backend | `firestore` (ready: `true`) |
| checks run | 11 — 11 passed, 0 failed, 0 unreachable |
| checks NOT run | 1 (see below — this receipt does NOT claim full coverage) |

`routes_registered` is the staleness tripwire: if the deployed control plane's route
count moves and this receipt was not re-run, the diff is the alarm.

## Checks RUN

| # | check | command | expected | observed | result |
|---|---|---|---|---|---|
| 1 | GET /readyz — control plane ready, backend genuinely reachable | `curl -sS --max-time 15 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/readyz'` | `200 · status="ready"` | `200 · status="ready"` | **PASS** |
| 2 | GET /config — presence timing contract served | `curl -sS --max-time 15 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/config'` | `200` | `200` | **PASS** |
| 3 | POST /corpus unsigned — PKI chokepoint refuses an unsigned write | `curl -sS --max-time 15 -X POST -H 'Content-Type: application/json' -d '{}' 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/corpus'` | `401 · error="missing_signature"` | `401 · error="missing_signature"` | **PASS** |
| 4 | GET /roster-team with NO team secret — outsider denied | `curl -sS --max-time 15 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/roster-team?project=github.com%2Frandomittin%2Fheimdall'` | `403 · error="team_secret_required"` | `403 · error="team_secret_required"` | **PASS** |
| 5 | GET /roster-team with a RANDOM secret — hashes to an empty partition, leaks no other tenant | `curl -sS --max-time 15 -H 'X-Heimdall-Team-Secret: <random 43-char secret>' 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/roster-team?project=github.com%2Frandomittin%2Fheimdall'` | `200 · online=[]` | `200 · online=[]` | **PASS** |
| 6 | GET /roster-public — the fully-public roster stays retired | `curl -sS --max-time 15 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/roster-public?project=github.com%2Frandomittin%2Fheimdall'` | `403 · error="roster_public_retired"` | `403 · error="roster_public_retired"` | **PASS** |
| 7 | GET /god/ on the public control plane — the god surface must be ABSENT, not merely unauthorized | `curl -sS --max-time 15 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/god/'` | `404 · error="no_such_route"` | `404 · error="no_such_route"` | **PASS** |
| 8 | GET /dispatch on the public control plane — dispatch surface absent | `curl -sS --max-time 15 'https://heimdall-cp-public-eqfrs7sfuq-uc.a.run.app/dispatch'` | `404 · error="no_such_route"` | `404 · error="no_such_route"` | **PASS** |
| 9 | GET /dashboard on the public site — the shipped dashboard is reachable | `curl -sS --max-time 15 'https://runheimdall.dev/dashboard'` | `200` | `200` | **PASS** |
| 10 | GET /god/ on the public site — no god surface on the marketing origin | `curl -sS --max-time 15 'https://runheimdall.dev/god/'` | `404` | `404` | **PASS** |
| 11 | bin/falsify rr-multitenant-isolation --assert-score 1.0 — every cross-tenant mutant killed | `bin/falsify rr-multitenant-isolation --assert-score 1.0` | `exit 0 · score 1.0 (golden passes, no mutant survives)` | `exit 0 · SCORE: 23/23 = 1.0000 (golden passing)` | **PASS** |

### Captured response bodies

- `readyz`:

  ```json
  {"status": "ready", "booted": true, "routes_registered": 35, "stores_reachable": true, "backend": "firestore", "backend_ready": true, "version": "1.0"}
  ```
- `config`:

  ```json
  {"beat_interval_s": 20, "refresh_interval_s": 15, "ttl_s": 45, "tier": 0}
  ```
- `corpus-unsigned`:

  ```json
  {"error": "missing_signature"}
  ```
- `roster-team-no-secret`:

  ```json
  {"error": "team_secret_required", "online": []}
  ```
- `roster-team-random-secret`:

  ```json
  {"project": "github.com/randomittin/heimdall", "online": []}
  ```
- `roster-public-retired`:

  ```json
  {"error": "roster_public_retired", "online": []}
  ```
- `cp-god`:

  ```json
  {"error": "no_such_route"}
  ```
- `cp-dispatch`:

  ```json
  {"error": "no_such_route"}
  ```

## Checks NOT run

### Check 4 of the canonical five — beat -> own watchman visible on /roster-team

**UNRUN (requires operator consent — production write)**

A presence beat WRITES a roster record to the live control plane. This verifier is read-only by construction and will never run it unattended. The written record self-expires after ttl_s (45s per /config), but it is still a production write, so it needs a human operator's consent. Its absence is why this receipt does NOT claim full coverage of the canonical five.

The exact command an operator would run:

```bash
cd <repo>
bin/heimdall-presence beat
bin/heimdall-presence roster --json | python3 -c "
import json,sys
d = json.load(sys.stdin)
haids = [e.get('haid') for e in d.get('online', [])]
print('HAIDs on roster:', haids)
assert haids, 'FAIL: empty roster - beat did not propagate'
print('PASS: %d entry/entries' % len(haids))
"
```
