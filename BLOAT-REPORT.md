# Bloat Report

_Generated by `heimdall-debloat` (spec H-2ii) at 2026-07-01T19:23:53Z_

- **Repo:** `/Users/rj/Downloads/heimdall`
- **Scope:** whole repository (`4b825dc642cb6eb9a060e54bf8d69288fbee4904..HEAD` — every tracked file scored as new code)
- **Files analyzed:** 460
- **Tracked LOC (added-line basis):** 120022

## Scorecard

| Axis | Result |
| --- | --- |
| Dead / unreferenced exports | SKIPPED — 0 dead symbol(s); no JS/TS dead-code tool on PATH (install knip or ts-prune) |
| Duplication clusters | SKIPPED — no duplication tool on PATH (install jscpd) |
| Complexity hotspots | SKIPPED — max cyclomatic 0; Python changed but radon not on PATH |
| Unused dependencies | measured (lockfile-diff) — 2 flagged dep line(s); lockfile(s) changed — new deps require plan-time approval (flag, not auto-fail) |
| Reimplemented stdlib/native (advisory) | measured (ast-grep) — 0 native-replaceable construct(s); native/stdlib already does this (e.g. JSON.parse(JSON.stringify(x)) -> structuredClone(x)) |

## Candidate-deletion LOC estimate

| Source | Estimated removable LOC |
| --- | --- |
| Dead code | 0 (axis skipped — not estimated) |
| Duplication | 0 (axis skipped — not estimated) |
| **Total candidate** | **0** |

> The estimate counts only what a deterministic tool actually proved removable.
> A skipped axis contributes 0 — an absent tool is an honest gap, never a guess.

## LOC-reduction cap

- Default cap: **20% of 120022 tracked LOC** ~= 24004 LOC
- Lift with `--aggressive` (use with care; larger PRs are harder to review).

## Not touched — manual review candidates

These are NEVER auto-deleted; static analysis cannot prove them dead:

- **Public API surface** — exported symbols consumed by downstream packages.
- **Dynamic usage** — reflection, string-keyed dispatch, `require(var)`, DI containers.
- **No-coverage regions** — code with no test exercising it; deleting it is unverifiable.
- **Generated code & migrations** — excluded by default (`exclude_globs` in .planning/bloat.json).

## Skipped axes (honest gaps)

- **dead**: skipped — no JS/TS dead-code tool on PATH (install knip or ts-prune)
- **duplication**: skipped — no duplication tool on PATH (install jscpd)
- **complexity**: skipped — Python changed but radon not on PATH

---
_To act on this report: re-run `heimdall-debloat` without `--report-only` (a passing
test suite is required, or use `--generate-characterization-tests` first)._
