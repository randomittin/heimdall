---
title: What the watchman learned, week ending 2026-08-03
date: 2026-08-03
slug: log-week-2026-08-03
description: 110 commits landed between 2026-07-28 and 2026-08-03. Every line below carries the sha it came from.
tags: [changelog, weekly]
author: Heimdall Engineering
status: draft
publish: false
generated: 2026-08-03T15:22:50Z
generator: bin/heimdall-weekly-log
window: 2026-07-28..2026-08-03
---

110 commits landed between 2026-07-28 and 2026-08-03. Each line carries the sha
it was read from, so any claim here can be checked with `git show`.

### Shipped

- feat(log): weekly changelog generator with consent + k-anonymity gates (`0970684`)
- feat(orchestrator): close idle agents automatically on idle_notification (`842e101`)
- feat(agents): evidence-based subagent states; parked teammates cannot be killed (`9ff8d7f`)
- feat(hooks): enforce R13 — deny named Agent spawns at PreToolUse (`20f78a2`)
- feat(schedule): status detects a nightly job pinned to a missing command (`7b72d58`)
- feat(install): distinct 'ephemeral' state for the worktree refusal (`c730aeb`)
- feat(assets): gate fixture-derived assets off reader-facing surfaces (`615ae8f`)
- feat(verify): read-only live isolation verifier + committed receipt (`663a845`)
- feat(funnel): derive launch-funnel stamps server-side (zero new client egress) (`bb1c576`)
- feat(oracles): register rr-multitenant-isolation in the oracle registry (`b2c2f6d`)
- feat(model): resolve latest Claude model per tier (alias) for operational spawns; keep bench pinned (`8ad4867`)
- feat(agents): track + reap stale/failed Agent-tool subagents (heimdall-agents + who + statusline count) (`168646b`)
- feat(agent-pool): reap stale-idle (alive-but-hung) agents via heartbeat threshold (`644759d`)
- feat(resilience): self-heal claude 529/overload across session-fork + autonomous loops (backoff+jitter+cap) (`eef3c98`)
- feat(team): heimdall-team-converge — converge-forward migration (dry-run, idempotent, preserves enrollments) (wave 3) (`2c4cd8b`)
- feat(statusline): show branch under every same-repo teammate (not only cross-branch) (`e112710`)
- feat(glama): glama.json manifest + Dockerfile.mcp for ledger-MCP indexing (`aa7c2b8`)
- feat(statusline): show same-repo teammates on other branches + branch line (`26cc12d`)
- feat(cp): enforce HAID key-possession on /team/auto (AT6 hardening) (`7f5ec0d`)
- feat(team): hmd team auto — GitHub zero-touch join/initiate; secret path is fallback (`4a84f67`)
- feat(presence): first-run auto-team via GitHub (replaces solo-mint; W3 client) (`390d0d8`)
- feat(cp): cp_autoteam — POST /team/auto zero-touch join/initiate (W2, oracle 21/21) (`c57131e`)
- feat(cp): cp_ghverify — server-side gh identity + repo-access strata (W1 of auto-team) (`9e1fa74`)
- feat(cp): cp_repoteam — server-side repo->team registry (W1 of auto-team) (`bee54b8`)
- feat(presence): network ON by default; opt out with 'sever' (RJ-approved, re-signed) (`68815b8`)
- feat(god): hmd god CLI (owner-only cross-team roster+logs) + owner-grant at boot (`ee27552`)

### Fixed

- fix(sysmon,skill): propagate the reboot correction to the other two advisory surfaces (`ee36b81`)
- fix(cleanup): a reboot is a temporary reclaim, not the fix — name the holder (`b31bf66`)
- fix(hooks): downgrade the named-agent gate from deny to warn (`9b03c86`)
- fix(agents): parked teammates are not restart-only — TaskStop and /tasks clear them (`3eb3b8a`)
- fix(agents): give every spawning agent TaskStop so it can close what it opens (`b214eb1`)
- fix(agents): completions are not orphans; see parked teammates; restore orphans (`753805f`)
- fix(deploy): refuse before minting a keychain credential in a sandbox (`970b4f4`)
- fix(schedule): refuse to pin the nightly job to an ephemeral checkout (`4ba6640`)
- fix(deploy): self-detecting sandbox guard on the maintainer CRON install path (`1db6c14`)
- fix(install): self-detecting sandbox guard on the LaunchAgent REGISTER path (`1686838`)
- fix(uninstall): reverse the nightly LaunchAgent + the ~/.claude statusLine (`b414f98`)
- fix(uninstall): stop the preamble resurrecting ~/.heimdall; surface the stranger suite (`57f0e17`)
- fix(selfscan): add tree-mode scan over the pushable set (`03c92ee`)
- fix(test): crypto hermeticity guard misattributed teammate edits to install.sh (`afd90e9`)
- fix(hooks): make the pre-push quality gate actually block (`8e28d08`)
- fix(verify-edits,summary-card): pure JSON, drop dead tripwire, fail closed on unreachable verifier (`161b2d5`)
- fix(telemetry,live-verify): green telemetry-install; fail closed on unresolved CP URL (`50cdf12`)
- fix(hmd): a missing bin/skill-manager must not kill `hmd --skills` (`b8d7ffc`)
- fix(hooks): stop echo from mangling JSON payloads into jq — gates were silently skipping (`0e7ee86`)
- fix(security): unblock selfscan gate — de-secret the C2 fixture, allowlist the one historical SHA (`91e2937`)
- fix(hmd): a missing heimdall-model-resolve must not abort the launcher (`b55c106`)
- fix(npm): re-sync npm README mirror — restore byte-identity with root (`257a704`)
- fix(summary-card): NON_VERIFIED is a first-class receipt state, not a green gate (`fcac19f`)
- fix(hooks): log edits from every file-mutating tool, not just Write|Edit (`47b5f49`)
- fix(test): bootstrap harness leaked setup state across scenarios (`93ed84e`)
- fix(version-drift): exclude gate scaffolding from the site sweep (`3bf291e`)
- fix(gates): kill vacuous-pass shapes in 5 checks — no green over an empty scan (`a638095`)
- fix(deploy): never install a crontab derived from a failed `crontab -l` (`0e7689a`)
- fix(verify-edits): absent edit ledger is NON-VERIFIED, not a pass (`1ad2961`)
- fix(install): repoint both READMEs at v2.3.8 — URL and digest now agree (`f9d4f30`)
- fix(docs): GO-LIVE-RUNBOOK probes /readyz; gate the edge-eaten-probe class repo-wide (`293ef51`)
- fix(oracles): remove phantom raytracer-calib entry, register ponytail-underdelivery, gate the README table (`9079a13`)
- fix(docs): probe /readyz not /healthz for public reachability (`2c0a6aa`)
- fix(readme): correct install.sh sha256 — the pinned digest aborted every install (`821dd86`)
- fix(gates): sweep the sibling site + implement version-drift --self-test (`9662990`)
- fix(agents): classify subagents from the transcript, not a .status file that never exists (`5e79c17`)
- fix(team-converge): fail-closed on cross-repo-shared team_ids (no ambiguous repoint) (`7f044a7`)
- fix(team-converge): firestore preflight — ADC/urllib3 checks + hard query timeout (no more silent hang) (`2178fe2`)
- fix(team-converge): print resolved backend + guard against silent local runs (`fd3aba4`)
- fix(team): /team/auto is the sole team authority for github repos — converge same-repo teammates (wave 2) (`9cffbbc`)
- fix(god): fall back to plain identity token for user accounts (--audiences fails) (`e299497`)

### Faster

- perf(landmine-lint): match per-line in bash, not printf|grep — 26s -> 9s (`30f289c`)

### Proven

- test(agents): close a false-green in the clearable_by length assertion (`f255143`)
- test(stranger): §8f — a genuine $HOME is not enough (the second mechanism) (`385dc8d`)
- test(deploy-maintainer): cover the crontab sandbox guard (20 new assertions) (`f04e163`)
- test(install-stranger): cover the install-side launchd sandbox guard (10 new assertions) (`04de055`)
- test(install-stranger): cover the two uninstall leftovers (13 new assertions) (`cc0e603`)
- test(runner): budget install-stranger at 300s (measured 93s solo) (`d25f35e`)
- test(runner): surface reuse-mini-git to the runner glob (253 -> 254) (`a832509`)
- test(issue-loop): assert open_pr's real contract, not a credentialed push (`2175bee`)
- test(runner): per-suite time budgets; raise default 120s -> 180s (`407e4f2`)
- test(suites): make the 3 presentation suites report assertion counts (`51fbc9a`)
- test(gates): class-level guard against echo-into-parser in the fail-open surface (`43a2402`)
- test(suites): make 10 unparseable suites report assertion counts (`9900358`)
- test(runner): single test runner — glob discovery, per-suite timeout, silent-red detection (`d9bc5b4`)
- test(secrets): make public-repo-no-secrets non-vacuous (count examined + falsifiability) (`1b9994c`)
- test(gates): gate pinned install digests against the bytes they describe (`930f413`)
- test(team): red convergence test — same repo_slug splits into two team_ids (wave 1 remainder) (`7576e3c`)
- test(oracle): AT7 same-repo-one-team convergence gate (23/23, falsifiable) (`3c6a0d4`)
- test(oracle): INV-AUTOTEAM AT6 — haid key-possession (hardening gate) (`3208b5f`)
- test(oracle): INV-AUTOTEAM — gate zero-touch auto-team-formation (AT1-AT5) (`4a1d1a3`)

### Documented

- docs(agents,skill): the named-agent hook warns, it does not deny (`efc5ac7`)
- docs(skill,coder): teach the idle-close rule; a named agent is not unkillable (`551c66a`)
- docs(agents): TaskStop closes named agents — correct the unkillable claim, add the idle-close rule (`24428ac`)
- docs(agents): teach R13-enforced spawn pattern — role identity in description:, not name: (`9b132d8`)
- docs(skill): note R13 is now hook-enforced, not advisory (`a3adb7f`)
- docs(readme): apply locked tagline; mark hero-recording insertion point (`f196ea8`)
- docs(launch): correct 3 misreported submission statuses; commit S6 sweep evidence (`b3deb33`)
- docs(log): D12 posts — deploy saga (corrected) + memory-with-receipts (`057a6f8`)
- docs: mark retired "no telemetry" absolute claim resolved (2026-07-28) (`b57a2b2`)
- docs(conventions): R13 — Agent spawns unnamed by default (`7f2f4af`)
- docs(listings): defer OpenAlternative — <10 stars + full-app gate (`2206c90`)
- docs(listings): §3 web-listing paste-sheet — expanded /ghost pitch, pre-fit per site (`a7b43e2`)
- docs(submissions): §2.1 — 497-char form-safe description (awesome-claude-code 500-char limit) (`b2794c1`)
- docs(submissions): mark §2.1 awesome-claude-code SUBMITTED (issue #2364) (`23b7a84`)
- docs(submissions): expand §2.1 pitch — cross-machine team orchestration, shared triage, checkpointing, /dream, design-match (`821379b`)
- docs(specs): commit the chat-originated design specs to docs/specs/ + Layer 0/1 index (`32f114e`)
- docs(submissions): lock RJ's per-item decisions + verification-first framing (`6a4d141`)

### Housekeeping

- chore(verify): record the first live isolation receipt (PASS 11/11) (`4b07a71`)
- chore(release): v2.3.8 (`063155f`)
- chore(release): v2.3.7 (`8303632`)
- chore(release): v2.3.6 (`2600cd8`)
- chore(release): v2.3.5 (`0df1c13`)
- chore(release): v2.3.4 (`7adeea4`)

## Credits

No fix this week carried a recorded team credit.

*Nothing ships unproven.*
