{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/parallel-gate\" 2>/dev/null || true"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "CTX=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/heimdall-ctx-meter\"; if [ -x \"$CTX\" ]; then \"$CTX\" notice; fi; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); CMD=$(printf '%s' \"$INPUT\" | jq -r '.tool_input.command // empty'); PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; SCAN=\"$PLUGIN/bin/secret-scan\"; GITGUARD=\"$PLUGIN/bin/heimdall-git-guard\"; if [ -x \"$GITGUARD\" ] && printf '%s\\n' \"$CMD\" | grep -qE '(^|[;&|]|[[:space:]])git([[:space:]]+-C[[:space:]]+[^[:space:]]+)?[[:space:]]+(add|commit|merge|rebase|stash|cherry-pick|revert|am|apply|pull)([[:space:]]|$)'; then \"$GITGUARD\"; fi; if printf '%s\\n' \"$CMD\" | grep -qE '(^|[;&|]\\s*)git\\s+commit'; then if [ -x \"$SCAN\" ]; then if ! OUT=$(\"$SCAN\" 2>&1); then ESC=$(printf '%s' \"$OUT\" | jq -Rsa . | sed -e 's/^\"//' -e 's/\"$//'); printf '{\"error\": \"BLOCKED: secret detected in staged changes by gitleaks. A credential must never enter history. Remove it from the staged diff before committing. --- %s\"}' \"$ESC\"; exit 2; else printf '%s\\n' \"$OUT\" >&2; fi; fi; fi; if printf '%s\\n' \"$CMD\" | grep -qE '^git\\s+push'; then HSTATE=\"$PLUGIN/bin/heimdall-state\"; [ -x \"$HSTATE\" ] || HSTATE=heimdall-state; QGOUT=$(\"$HSTATE\" check-quality-gates 2>&1); QGRC=$?; if [ \"$QGRC\" = 2 ]; then ESC=$(printf '%s' \"$QGOUT\" | jq -Rsa . | sed -e 's/^\"//' -e 's/\"$//'); printf '{\"error\": \"BLOCKED: pre-push quality gate failed. CLAUDE.md enforces quality gates before git push: all tests passing, lint clean, no untested changes. Fix the failing gate, re-run the suite, then record the result with `heimdall-state mark-clean` before pushing. --- %s\"}' \"$ESC\"; exit 2; elif [ \"$QGRC\" = 0 ]; then printf '%s\\n' \"$QGOUT\" >&2; else printf 'heimdall: quality-gate verdict UNAVAILABLE (exit %s) - the gates were NEVER EVALUATED, which is not a pass. Run `heimdall-state init` then `heimdall-state mark-clean` in this checkout to arm the pre-push gate. Allowing push to proceed. --- %s\\n' \"$QGRC\" \"$QGOUT\" >&2; fi; if [ -x \"$SCAN\" ]; then if ! OUT=$(SECRET_SCAN_REQUIRE=1 \"$SCAN\" --require 2>&1); then ESC=$(printf '%s' \"$OUT\" | jq -Rsa . | sed -e 's/^\"//' -e 's/\"$//'); printf '{\"error\": \"BLOCKED: pre-push secret gate failed (gitleaks). A secret in the push range or a missing scanner blocks the push \u2014 gitleaks must be present and the range clean. --- %s\"}' \"$ESC\"; exit 2; else printf '%s\\n' \"$OUT\" >&2; fi; fi; SELFSCAN=\"$PLUGIN/bin/heimdall-selfscan\"; if [ -x \"$SELFSCAN\" ]; then if ! OUT=$(\"$SELFSCAN\" 2>&1); then ESC=$(printf '%s' \"$OUT\" | jq -Rsa . | sed -e 's/^\"//' -e 's/\"$//'); printf '{\"error\": \"BLOCKED: pre-push self-scan failed (gitleaks over Heimdall OWN full history). A secret anywhere in this repo history, or a missing scanner, blocks the push. Agents commit --no-verify (bypassing the pre-commit gate), so this full-history self-scan is the backstop. Scrub the secret from history (git filter-repo) or install gitleaks. --- %s\"}' \"$ESC\"; exit 2; else printf '%s\\n' \"$OUT\" >&2; fi; fi; NHP=$(git config --get core.hooksPath 2>/dev/null || true); NATIVE_GATE=0; if [ \"${HMD_SKIP:-0}\" != \"1\" ] && [ \"$NHP\" = \".heimdall/hooks\" ] && [ -x \".heimdall/hooks/pre-push\" ]; then NATIVE_GATE=1; printf 'heimdall: oracle+corpus gates delegated to the native pre-push hook (.heimdall/hooks/pre-push runs heimdall-gate-run) — skipping the duplicate inline run.\\n' >&2; fi; FALSIFY=\"$PLUGIN/bin/falsify\"; ORACLES=\"$PLUGIN/evals/oracles\"; if [ \"$NATIVE_GATE\" = 0 ] && [ -x \"$FALSIFY\" ] && [ -d \"$ORACLES\" ]; then for D in \"$ORACLES\"/*/; do DOM=$(basename \"$D\"); if [ -d \"$D/fixtures/mutants\" ]; then APPLIES=0; if [ -f .planning/detected-stack.json ] && grep -q \"$DOM\" .planning/detected-stack.json 2>/dev/null; then APPLIES=1; fi; if [ -d \"evals/oracles/$DOM\" ]; then APPLIES=1; fi; if [ \"$APPLIES\" = 1 ]; then if ! \"$FALSIFY\" \"$DOM\" --assert-score 1.0 >&2; then STAMP=\"$PLUGIN/bin/heimdall-stamp\"; REP=\"evals/oracles/$DOM/report.json\"; if [ -x \"$STAMP\" ]; then if [ -f \"$REP\" ]; then \"$STAMP\" --report \"$REP\" --branch \"heimdall/blocked/$DOM\" >&2 2>/dev/null || true; else \"$STAMP\" --violation \"oracle gate $DOM is not falsifiable (score < 1.0)\" --branch \"heimdall/blocked/$DOM\" >&2 2>/dev/null || true; fi; fi; printf '{\"error\": \"BLOCKED: oracle gate %s is not falsifiable (score < 1.0). A green test suite over a non-falsifiable gate must not pass push. Harden the gate until bin/falsify reports 1.0.\"}' \"$DOM\"; exit 2; fi; fi; fi; done; fi; CORPUS=\"$PLUGIN/bin/corpus\"; if [ \"$NATIVE_GATE\" = 0 ] && [ -f evals/corpus/INDEX.json ] && [ -x \"$CORPUS\" ]; then if ! \"$CORPUS\" run >&2; then STAMP=\"$PLUGIN/bin/heimdall-stamp\"; [ -x \"$STAMP\" ] && \"$STAMP\" --violation \"corpus regression \u2014 a case the gates once caught is no longer caught\" --branch \"heimdall/blocked/corpus\" >&2 2>/dev/null || true; printf '{\"error\": \"BLOCKED: corpus regression \u2014 bin/corpus run is nonzero. A case the gates once caught is no longer caught (CI inversion, H-8). The gates are regression-tested by the failures they once caught; restore 100%% catch before push.\"}'; exit 2; fi; fi; fi; \"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/parallelism-tracker\" check Bash 2>&1 || true"
          }
        ]
      },
      {
        "matcher": "Read|Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); TOOL=$(printf '%s' \"$INPUT\" | jq -r '.tool_name // \"unknown\"' 2>/dev/null || echo unknown); \"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/parallelism-tracker\" check \"$TOOL\" 2>&1 || true"
          }
        ]
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; \"$PLUGIN/bin/parallelism-tracker\" check Agent 2>&1 || true; printf '%s' \"$INPUT\" | \"$PLUGIN/bin/heimdall-precheck-agent\""
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); CONTENT=$(printf '%s' \"$INPUT\" | jq -r '.tool_input.content // .tool_input.new_string // empty' 2>/dev/null); TOOL=$(printf '%s' \"$INPUT\" | jq -r '.tool_name // \"unknown\"' 2>/dev/null || echo unknown); FPATH=$(printf '%s' \"$INPUT\" | jq -r '.tool_input.file_path // empty' 2>/dev/null); \"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/parallelism-tracker\" check \"$TOOL\" 2>&1 || true; if [ -z \"$CONTENT\" ]; then exit 0; fi; emit(){ MSG=$(jq -cn --arg s \"$1\" --arg p \"${FPATH:-<unknown path>}\" '{error: (\"\ud83d\uded1 BIFR\u00d6ST \u2014 Heimdall blocked a stub: \" + $s + \" @ \" + $p + \". Nothing ships unproven. Write a real implementation or explain why you cannot \u2014 this gate matches CODE SHAPE, not keywords (prose/docs *.md/*.txt, data *.json/*.ndjson, and fixtures/ or evals/ paths are exempt).\")}'); printf '%s\\n' \"$MSG\"; printf '%s\\n' \"$MSG\" >&2; exit 2; }; PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; STUBLIB=\"$PLUGIN/bin/lib/heimdall-stub-patterns.sh\"; if [ ! -f \"$STUBLIB\" ] || ! . \"$STUBLIB\" 2>/dev/null; then emit \"stub-scanner unavailable (shared pattern library failed to load) \u2014 failing closed\"; fi; if heimdall_stub_exempt_path \"${FPATH:-}\"; then exit 0; fi; SHAPE=$(printf '%s' \"$CONTENT\" | heimdall_stub_match); if [ -n \"$SHAPE\" ]; then emit \"$SHAPE\"; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; printf '%s' \"$INPUT\" | \"$PLUGIN/bin/heimdall-precheck-edit\" || true"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; CAP=\"$PLUGIN/bin/corpus-capture\"; if [ -x \"$CAP\" ] && [ -d evals/oracles ] && command -v jq >/dev/null 2>&1; then for R in evals/oracles/*/report.json; do [ -f \"$R\" ] || continue; if [ \"$(jq -r '.status // empty' \"$R\" 2>/dev/null)\" = \"fail\" ]; then \"$CAP\" --report \"$R\" 2>/dev/null || true; fi; done; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); TOOL=$(printf '%s' \"$INPUT\" | jq -r '.tool_name // \"unknown\"'); FPATH=$(printf '%s' \"$INPUT\" | jq -r '.tool_input.file_path // .tool_input.notebook_path // empty'); if [ -n \"$FPATH\" ]; then \"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/edit-tracker\" log \"$TOOL\" \"$FPATH\" 2>/dev/null || true; fi; if [ -f heimdall-state.json ]; then heimdall-state mark-dirty 2>/dev/null || true; elif [ -f superx-state.json ]; then heimdall-state mark-dirty 2>/dev/null || true; fi; if [ -f .heimdall-no-autocommit ] || [ -f .superx-no-autocommit ]; then exit 0; fi; count=$(git diff --name-only 2>/dev/null | wc -l | tr -d ' '); if [ \"$count\" -ge 5 ] && git rev-parse --git-dir >/dev/null 2>&1; then AC=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/heimdall-autocommit\"; if [ -x \"$AC\" ]; then \"$AC\" \"auto-checkpoint\" 2>/dev/null || true; fi; fi"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; CTXSYNC=\"$PLUGIN/bin/heimdall-context-sync\"; if [ -x \"$CTXSYNC\" ] && [ -d .git ]; then ( \"$CTXSYNC\" sync --repo \"${CLAUDE_PROJECT_DIR:-.}\" --throttle 900 >/dev/null 2>&1 & ) ; fi; exit 0"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; AUTOUP=\"$PLUGIN/bin/heimdall-autoupdate\"; [ -x \"$AUTOUP\" ] && (\"$AUTOUP\" check >/dev/null 2>&1 &) || true; SLREG=\"$PLUGIN/bin/heimdall-statusline-register\"; [ -x \"$SLREG\" ] && \"$SLREG\" register >/dev/null || true; HEAL=\"$PLUGIN/bin/heimdall-cc-selfheal\"; if [ -x \"$HEAL\" ]; then (\"$HEAL\" check >/dev/null 2>&1 &) || true; \"$HEAL\" pin-notice 2>/dev/null || true; fi; TRACKER=\"$PLUGIN/bin/parallelism-tracker\"; if [ -f \"$PLUGIN/bin/parallelism-tracker.c\" ] && { [ ! -x \"$TRACKER\" ] || ! \"$TRACKER\" check probe >/dev/null 2>&1; }; then \"$PLUGIN/bin/build-tracker.sh\" >/dev/null 2>&1 || true; fi; ETRACKER=\"$PLUGIN/bin/edit-tracker\"; if [ -f \"$PLUGIN/bin/edit-tracker.c\" ] && { [ ! -x \"$ETRACKER\" ] || [ \"$PLUGIN/bin/edit-tracker.c\" -nt \"$ETRACKER\" ]; }; then clang -O2 -Wall -Wextra -o \"$ETRACKER\" \"$PLUGIN/bin/edit-tracker.c\" 2>/dev/null || true; fi; \"$ETRACKER\" clear 2>/dev/null || true; \"$ETRACKER\" init 2>/dev/null || true; if [ ! -f heimdall-state.json ] && [ ! -f superx-state.json ]; then heimdall-state init 2>/dev/null || true; fi; if [ -f heimdall-state.json ] || [ -f superx-state.json ] || [ -d .planning ]; then mkdir -p .planning 2>/dev/null && \"$PLUGIN/bin/stack-pack\" detect 2>/dev/null > .planning/detected-stack.json || true; fi; HMDTEAM=\"$PLUGIN/bin/heimdall-team\"; HMDPRES=\"$PLUGIN/bin/heimdall-presence\"; if [ -x \"$HMDTEAM\" ] || [ -x \"$HMDPRES\" ]; then ( [ -x \"$HMDTEAM\" ] && \"$HMDTEAM\" auto >/dev/null 2>&1; [ -x \"$HMDPRES\" ] && \"$HMDPRES\" beat >/dev/null 2>&1; ) >/dev/null 2>&1 & fi; REAP=\"$PLUGIN/bin/heimdall-reap-idle\"; if [ -x \"$REAP\" ] && [ -d .git ]; then RN=$(\"$REAP\" 2>/dev/null | grep -cE '^\\[REAP' || true); [ \"${RN:-0}\" -gt 0 ] && echo \"[heimdall] $RN agent worktree(s) reapable — run bin/heimdall-reap-idle --apply to reclaim disk (a worktree holding uncommitted changes, untracked files, agent memory, or unmerged commits is never removed).\"; fi; CLEAN=\"$PLUGIN/bin/heimdall-cleanup\"; GC=\"$PLUGIN/bin/heimdall-gc\"; if [ -x \"$CLEAN\" ]; then \"$CLEAN\" --advise --quick --repo \"${CLAUDE_PROJECT_DIR:-.}\" 2>/dev/null || true; (\"$CLEAN\" --auto --quick --repo \"${CLAUDE_PROJECT_DIR:-.}\" >/dev/null 2>&1 &) || true; elif [ -x \"$GC\" ]; then (\"$GC\" run --quick --quiet >/dev/null 2>&1 &) || true; fi; if [ -f .planning/CHECKPOINT.md ]; then echo \"[heimdall] Checkpoint found \u2014 read .planning/CHECKPOINT.md to resume.\"; elif [ -f .planning/STATE.md ]; then echo \"[heimdall] State found \u2014 read .planning/STATE.md for prior context.\"; fi"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; RPROBE=\"$PLUGIN/bin/heimdall-resume-probe\"; RPDIR=\"${CLAUDE_PROJECT_DIR:-.}\"; if [ -x \"$RPROBE\" ] && [ -f \"$RPDIR/.planning/CHECKPOINT.md\" ]; then RPOUT=\"$(mktemp 2>/dev/null || printf '/tmp/hmd-resume-probe.%s' \"$$\")\"; if command -v perl >/dev/null 2>&1; then perl -e 'alarm 10; exec @ARGV' \"$RPROBE\" run --repo \"$RPDIR\" >\"$RPOUT\" 2>/dev/null; else \"$RPROBE\" run --repo \"$RPDIR\" >\"$RPOUT\" 2>/dev/null; fi; head -8 \"$RPOUT\" 2>/dev/null; rm -f \"$RPOUT\"; fi; exit 0"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/heimdall-maintain-loop\" resume-hint --repo \"${CLAUDE_PROJECT_DIR:-.}\" 2>/dev/null || true"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/bin/heimdall-quota-resume\" resume-hint --repo \"${CLAUDE_PROJECT_DIR:-.}\" 2>/dev/null || true"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; NOTICE=\"$PLUGIN/bin/heimdall-dream-notice\"; [ -x \"$NOTICE\" ] && \"$NOTICE\" --repo \"${CLAUDE_PROJECT_DIR:-.}\" 2>/dev/null || true"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; HP=\"$PLUGIN/bin/heimdall-presence\"; if [ -x \"$HP\" ]; then SID=$(printf '%s' \"$INPUT\" | jq -r '.session_id // empty' 2>/dev/null || true); (\"$HP\" keeper-start --session \"$SID\" >/dev/null 2>&1 &) ; fi; exit 0"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; HP=\"$PLUGIN/bin/heimdall-presence\"; [ -x \"$HP\" ] && \"$HP\" connect-prompt 2>/dev/null || true"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; AISEL=\"$PLUGIN/bin/heimdall-ai-select\"; [ -x \"$AISEL\" ] && \"$AISEL\" session-start \"${CLAUDE_PROJECT_DIR:-.}\" 2>/dev/null || true"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; HOOK=\"$PLUGIN/bin/heimdall-metric-hook\"; if [ -x \"$HOOK\" ]; then \"$HOOK\" stop --repo \"${CLAUDE_PROJECT_DIR:-.}\"; fi; exit 0"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; HP=\"$PLUGIN/bin/heimdall-presence\"; if [ -x \"$HP\" ]; then SID=$(printf '%s' \"$INPUT\" | jq -r '.session_id // empty' 2>/dev/null || true); \"$HP\" keeper-stop --session \"$SID\" >/dev/null 2>&1 || true; fi; exit 0"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN=\"${CLAUDE_PLUGIN_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}\"; export PLUGIN; _hmd_to() { if command -v timeout >/dev/null 2>&1; then timeout 10s \"$@\"; elif command -v gtimeout >/dev/null 2>&1; then gtimeout 10s \"$@\"; else \"$@\"; fi; }; _hmd_to bash -c 'CKPT=\"$PLUGIN/bin/heimdall-checkpoint\"; if [ -x \"$CKPT\" ] && { [ -d .planning ] || [ -f heimdall-state.json ] || [ -f superx-state.json ]; }; then \"$CKPT\" write . >/dev/null 2>&1 || true; fi; \"$PLUGIN/bin/parallelism-tracker\" grade 2>&1 || true; \"$PLUGIN/bin/verify-edits\" --quick 2>&1 || true; if [ ! -f .heimdall-no-autocommit ] && [ ! -f .superx-no-autocommit ] && git rev-parse --git-dir >/dev/null 2>&1 && [ -n \"$(git status --porcelain 2>/dev/null)\" ]; then if [ -x \"$PLUGIN/bin/heimdall-autocommit\" ]; then \"$PLUGIN/bin/heimdall-autocommit\" \"session-end checkpoint\" >/dev/null 2>&1 || true; fi; fi' || true; ( CLEAN=\"$PLUGIN/bin/heimdall-cleanup\"; GC=\"$PLUGIN/bin/heimdall-gc\"; if [ -x \"$CLEAN\" ]; then \"$CLEAN\" --auto --repo \"${CLAUDE_PROJECT_DIR:-.}\" >/dev/null 2>&1; elif [ -x \"$GC\" ]; then \"$GC\" run --quiet >/dev/null 2>&1; else REAP=\"$PLUGIN/bin/heimdall-reap-idle\"; [ -x \"$REAP\" ] && [ -d .git ] && \"$REAP\" --apply >/dev/null 2>&1; fi; if [ -x \"$PLUGIN/bin/heimdall-reel\" ] && { [ -d .planning ] || [ -f heimdall-state.json ] || [ -f superx-state.json ]; }; then \"$PLUGIN/bin/heimdall-reel\" record . --name \"run-$(date +%Y%m%d-%H%M%S 2>/dev/null || echo run)\" >/dev/null 2>&1; fi; \"$PLUGIN/bin/summary-card\" --receipt . >/dev/null 2>&1; CTXSYNC=\"$PLUGIN/bin/heimdall-context-sync\"; if [ -x \"$CTXSYNC\" ] && [ -d .git ]; then _hmd_to \"$CTXSYNC\" sync --repo \"${CLAUDE_PROJECT_DIR:-.}\" >/dev/null 2>&1 || true; fi; ) >/dev/null 2>&1 & FAREWELL=\"$PLUGIN/sentinels/hmd-farewell.sh\"; [ -x \"$FAREWELL\" ] && bash \"$FAREWELL\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}