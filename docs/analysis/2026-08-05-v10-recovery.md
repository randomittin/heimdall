# V10 recovered — the VIRALITY WORK ORDER's missing item

**Answer: V10 is "Content batch (drafts staged for RJ's editorial pass)."** Not inferred —
quoted from the owner's own paste.

## Source (single hop, reproducible)

`/Users/rj/.claude/projects/-Users-rj-Downloads-heimdall/da3a8887-1f95-4283-b2e0-38175ca264e5.jsonl`
**line 7425** — record `type:"user"`, `origin.kind:"human"`, `promptSource:"typed"`,
uuid `a31c772d-9ca5-45ee-a87e-cd68906fd494`, ts `2026-08-03T14:55:30.862Z`,
cwd `/Users/rj/Downloads/heimdall`, branch `main`, version `2.1.220`.

```
grep -n '## V10' /Users/rj/.claude/projects/-Users-rj-Downloads-heimdall/da3a8887-1f95-4283-b2e0-38175ca264e5.jsonl
```

The record holds the ENTIRE V1–V12 order plus the order line, so V10's position is confirmed,
not reconstructed: it sits between **V9. Funnel + metrics report** and
**V11. ACP adapter scoping ⚠RJ-APPROVE**.

## Verbatim

> ## V10. Content batch (drafts staged for RJ's editorial pass)
> (a) Deploy-saga post final ("8 bugs that only existed in production") + dev.to canonical version. (b) Almanac counter-post ("unverified agent memory is faster-rotting docs — memory with receipts"). (c) The OpenHands complementary post (V3's long form). (d) Show HN draft: 3 title options (receipts-shaped), body, first-comment — using the PH maker-comment voice.
> Gate: every claim in every draft traces to a repo artifact or a cohort receipt; staged in launch-docs/, nothing published.

Order line, verbatim: `## Order: V1 → V5 → V9 → V8 → V6/V7 → V3/V4 → V10 → V12-staged, with
V2/V11 firing the moment RJ says the word.`

## Artifacts on disk (measured 2026-08-05)

| sub-item | file | lines | commit |
|---|---|---|---|
| (a) | `launch-docs/log-deploy-saga.md` + `launch-docs/devto-deploy-saga.md` | 246 + 147 | `057a6f8` |
| (b) | `launch-docs/log-memory-with-receipts.md` | 208 | `057a6f8` |
| (c) | `launch-docs/log-runner-and-gate.md` | 191 | `89dcc77`, then `5bbbdce`/`b5543e3` |
| (d) | `launch-docs/SHOW-HN-DRAFT.md` ("## 1. Title — three options" at L20) | 196 | `89dcc77` |

Attribution is by content match to the work order's own sub-item letters; no commit carries a
`V10` tag. **V10's gate is NOT green:** `SHOW-HN-DRAFT.md` still holds **16** `RECEIPT`
markers, so not every claim traces to an artifact yet. Nothing is published — the gate's
second half holds. Re-measured: `log-runner-and-gate.md` now has **0** UNVERIFIED; the
checkpoint's "3 UNVERIFIED" is stale (fixed by `5bbbdce`).

## Why the previous audit said CANNOT-DETERMINE

The paste lives in the *same* session file the audit was itself running in, not an archived
one. Hunting `heimdall-path-to-viral.md` or a `V10` git tag returns nothing; grepping the raw
JSONL for `## V10` or `Order: V1` returns it immediately.

## Where this pass searched

- All 100 project dirs under `/Users/rj/.claude/projects/` (617 MB), recursive over every
  `*.jsonl` incl. `subagents/` and `tool-results/` — patterns: `VIRALITY WORK ORDER`,
  `WORK ORDER`, `RJ-APPROVE`, `Order: V1`, `V10.`, `V10:`, `## V10`, `**V10`, `V-10`,
  `V11.`, `V12.`. Only hits outside session `da3a8887`: SVG path data
  (`.../-Users-rj--claude-mem-observer-sessions/cc2168d1....jsonl`,
  `.../-Users-rj-Downloads-code-rally/2bbaf1fb.../subagents/agent-a617c44db0c2e6d9d.jsonl`) —
  false positives on `V10.` inside path coordinates.
- `/Users/rj/Downloads/heimdall/launch-docs/`, `docs/`, `.planning/` (incl. gitignored files).
- `git log --all` for the four candidate artifacts.

Full text of the recovered item is also reproduced in `.planning/CHECKPOINT.md` (gitignored).
